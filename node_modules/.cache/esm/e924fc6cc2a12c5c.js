let minimist,version,help,configure,now,next;_c2a‍.x([["cli",()=>cli]]);_c2a‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_c2a‍.w("./version",[["version",["version"],function(v){version=v}]]);_c2a‍.w("./help",[["help",["help"],function(v){help=v}]]);_c2a‍.w("./configure",[["configure",["configure"],function(v){configure=v}]]);_c2a‍.w("./now",[["now",["now"],function(v){now=v}]]);_c2a‍.w("./next",[["next",["next"],function(v){next=v}]]);






       async function cli(argsArray) {
  const args = minimist(argsArray.slice(2));
  let cmd = args._[0] || 'help';
// if flag is --version or -v , gets npm version
  if (args.version || args.v) {
    cmd = 'version';
  }

  if (args.help || args.h) {
    cmd = 'help';
  }

  switch (cmd) {
    case 'version':
      version(args);
      break;

    case 'help':
      help(args);
      break;
    
    case 'config':
      configure(args);
      console.log("token accepted");
      break;

    case 'now':
      now(args);
      break;

    case 'next':
      next(args);
      break;

    default:
      console.error(`"${cmd}" is not a valid command!`);
      break;
  }
}