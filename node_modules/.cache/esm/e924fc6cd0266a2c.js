let minimist,version,help,configure,now;_d02‍.x([["cli",()=>cli]]);_d02‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_d02‍.w("./version",[["version",["version"],function(v){version=v}]]);_d02‍.w("./help",[["help",["help"],function(v){help=v}]]);_d02‍.w("./configure",[["configure",["configure"],function(v){configure=v}]]);_d02‍.w("./now",[["now",["now"],function(v){now=v}]]);





       async function cli(argsArray) {
  const args = minimist(argsArray.slice(2));
  let cmd = args._[0] || 'help';
  // if flag is --version or -v , gets npm version
  if (args.version || args.v) {
    cmd = 'version';
  }

  if (args.help || args.h) {
    cmd = 'help';
  }

  switch (cmd) {
    case 'version':
      version(args);
      break;

    case 'help':
      help(args);
      break;
    
    case 'config':
        console.log("hi");
      configure(args);
      
      break;

    case 'now':
      now(args);
      break;

    default:
      console.error(`"${cmd}" is not a valid command!`);
      break;
  }
}