let minimist,version,help,configure,now,next;_59d‍.x([["cli",()=>cli]]);_59d‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_59d‍.w("./version",[["version",["version"],function(v){version=v}]]);_59d‍.w("./help",[["help",["help"],function(v){help=v}]]);_59d‍.w("./configure",[["configure",["configure"],function(v){configure=v}]]);_59d‍.w("./now",[["now",["now"],function(v){now=v}]]);_59d‍.w("./next",[["next",["next"],function(v){next=v}]]);






// export let configThisStep = "thisStep";
// let step = 0;

       async function cli(argsArray) {
  const args = minimist(argsArray.slice(2));
  
  let cmd = args._[0] || 'help';
// if flag is --version or -v , gets npm version
  if (args.version || args.v) {
    cmd = 'version';
  }

  if (args.help || args.h) {
    cmd = 'help';
  }

  switch (cmd) {
    case 'version':
      version(args);
      break;

    case 'help':
      help(args);
      break;
    
    case 'config':
      configure(args);
      console.log("token accepted");
      break;

    case 'now':
      now(args);
      break;

    case 'next':
      next(args);
      break;

    default:
      console.error(`"${cmd}" is not a valid command!`);
      break;
  }
}