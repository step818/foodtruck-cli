let minimist,version,help,configure,now;_6c7‍.x([["cli",()=>cli]]);_6c7‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_6c7‍.w("./version",[["version",["version"],function(v){version=v}]]);_6c7‍.w("./help",[["help",["help"],function(v){help=v}]]);_6c7‍.w("./configure",[["configure",["configure"],function(v){configure=v}]]);_6c7‍.w("./now",[["now",["now"],function(v){now=v}]]);





       async function cli(argsArray) {
  const args = minimist(argsArray.slice(2));
  let cmd = args._[0] || 'help';
  // if flag is --version or -v , gets npm version
  if (args.version || args.v) {
    cmd = 'version';
  }

  if (args.help || args.h) {
    cmd = 'help';
  }

  switch (cmd) {
    case 'version':
      version(args);
      break;

    case 'help':
      help(args);
      break;
    
    case 'config':
        console.log("hi");
      configure(args);
      
      break;

    case 'now':
      now(args);
      break;

      case 'next':
        next(args);
        break;

    default:
      console.error(`"${cmd}" is not a valid command!`);
      break;
  }
}