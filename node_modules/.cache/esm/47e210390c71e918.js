let axios,chalk;_0c7‍.x([["validateApiKey",()=>validateApiKey],["queryFoodTrucks",()=>queryFoodTrucks]]);_0c7‍.w("axios",[["default",["axios"],function(v){axios=v}]]);_0c7‍.w("chalk",[["default",["chalk"],function(v){chalk=v}]]);


       function validateApiKey(apiKey) {
  if (!apiKey) {
    _0c7‍.g.console.error(
      chalk.redBright(
        `API Key has not been set up yet.`
      )
    );
    console.warn(
      `Please use command ${chalk.greenBright(
        'show-open-food-trucks config --apiKey <apiKey>'
      )} to save your API key.
      `
    );
    return false;
  }
  return true;
}

       async function queryFoodTrucks(apiKey) {
  const baseUri = 'https://data.sfgov.org/resource/jjew-r69b.json';
  const url = getApiUrl(baseUri, apiKey);
  return await axios({
    method: 'get',
    url: url
  });
}

function getApiUrl(baseUri, apiKey) {
  let url = baseUri + `?$$app_token=${apiKey.trim()}`;
  // url += `&$$app_token=${apiKey.trim()}`;
  return url;
}