let Conf,validateApiKey;_850‍.x([["configKey",()=>configKey],["configure",()=>configure]]);_850‍.w("conf",[["default",["Conf"],function(v){Conf=v}]]);_850‍.w("./utils",[["validateApiKey",["validateApiKey"],function(v){validateApiKey=v}]]);


       const configKey = 'foodtruck-cli';

       async function configure(args) {
  const config = new Conf();
  let currentConfigObject = config.get(configKey);
  currentConfigObject = currentConfigObject || {};

  let apiKey = args.apiKey;
  if (!apiKey) {
    apiKey = currentConfigObject.apiKey;
  }
  if (!validateApiKey(apiKey)) {
    return;
  }

  config.set(configKey, { apiKey: apiKey });
}