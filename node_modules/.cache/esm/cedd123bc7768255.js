let Conf,validateApiKey;_c77‍.x([["configKey",()=>configKey],["configure",()=>configure]]);_c77‍.w("conf",[["default",["Conf"],function(v){Conf=v}]]);_c77‍.w("./utils",[["validateApiKey",["validateApiKey"],function(v){validateApiKey=v}]]);


       const configKey = 'foodtruck-cli';

       async function configure(args) {
  const config = new Conf();
  let currentConfigObject = config.get(configKey);
  currentConfigObject = currentConfigObject || {};

  let apiKey = args.apiKey ? args.apiKey : args.k;
  // let apiKey = args.apiKey || args.k;
  if (!apiKey) {
    apiKey = currentConfigObject.apiKey;
  }
  if (!validateApiKey(apiKey)) {
    return;
  }

  config.set(configKey, { apiKey: apiKey });
}